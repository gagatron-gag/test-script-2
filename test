local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ErvoHub"
ScreenGui.ResetOnSpawn = false

pcall(function()
	ScreenGui.Parent = game:GetService("CoreGui")
end)

if not ScreenGui.Parent then
	ScreenGui.Parent = player:WaitForChild("PlayerGui")
end

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 20, 28)
MainFrame.Position = UDim2.new(0.5, -115, 0.5, -85)
MainFrame.Size = UDim2.new(0, 230, 0, 170)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.ClipsDescendants = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Thickness = 1.5
Stroke.Color = Color3.fromRGB(0, 170, 255)
Stroke.Transparency = 0.3

local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundColor3 = Color3.fromRGB(10, 12, 18)
Header.BorderSizePixel = 0
Header.Active = true
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(1, 0, 1, 0)
Title.Text = " Ervo Hub ⚡"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 14
Title.TextColor3 = Color3.fromRGB(180, 220, 255)
Title.BackgroundTransparency = 1

local Close = Instance.new("TextButton", Header)
Close.Size = UDim2.new(0, 25, 0, 25)
Close.Position = UDim2.new(1, -30, 0.5, -12)
Close.Text = "❌"
Close.Font = Enum.Font.GothamBold
Close.TextSize = 14
Close.TextColor3 = Color3.fromRGB(220, 220, 220)
Close.BackgroundTransparency = 1

local Minimize = Instance.new("TextButton", Header)
Minimize.Size = UDim2.new(0, 25, 0, 25)
Minimize.Position = UDim2.new(1, -55, 0.5, -12)
Minimize.Text = "—"
Minimize.Font = Enum.Font.GothamBold
Minimize.TextSize = 14
Minimize.TextColor3 = Color3.fromRGB(220, 220, 220)
Minimize.BackgroundTransparency = 1

local Content = Instance.new("Frame", MainFrame)
Content.Size = UDim2.new(1, 0, 1, -35)
Content.Position = UDim2.new(0, 0, 0, 35)
Content.BackgroundTransparency = 1

local Username = Instance.new("TextLabel", Content)
Username.Size = UDim2.new(1, 0, 0, 20)
Username.Position = UDim2.new(0, 0, 0, 10)
Username.Text = "Client User : " .. player.Name
Username.Font = Enum.Font.GothamBold
Username.TextSize = 12
Username.TextColor3 = Color3.fromRGB(0, 200, 255)
Username.BackgroundTransparency = 1
Username.TextXAlignment = Enum.TextXAlignment.Center

local function Create3DButton(parent, text, pos, size, color, textColor)
	local Shadow = Instance.new("Frame", parent)
	Shadow.Size = size
	Shadow.Position = pos
	Shadow.BackgroundColor3 = color:Lerp(Color3.new(0, 0, 0), 0.45)
	Shadow.BorderSizePixel = 0
	Instance.new("UICorner", Shadow).CornerRadius = UDim.new(0, 8)

	local Top = Instance.new("TextButton", Shadow)
	Top.Size = UDim2.new(1, 0, 1, 0)
	Top.Position = UDim2.new(0, 0, 0, -3)
	Top.BackgroundColor3 = color
	Top.Text = text
	Top.Font = Enum.Font.GothamBold
	Top.TextSize = 13
	Top.TextColor3 = textColor or Color3.new(1, 1, 1)
	Top.AutoButtonColor = false
	Top.BorderSizePixel = 0
	Instance.new("UICorner", Top).CornerRadius = UDim.new(0, 8)

	Top.MouseButton1Down:Connect(function()
		TweenService:Create(Top, TweenInfo.new(0.08, Enum.EasingStyle.Quad), {Position = UDim2.new(0, 0, 0, 0)}):Play()
	end)

	Top.MouseButton1Up:Connect(function()
		TweenService:Create(Top, TweenInfo.new(0.08, Enum.EasingStyle.Quad), {Position = UDim2.new(0, 0, 0, -3)}):Play()
	end)

	return Top, Shadow
end

local MaskTop, MaskShadow = Create3DButton(Content, "Trade Masker : OFF", UDim2.new(0.075, 0, 0, 35), UDim2.new(0.85, 0, 0, 32), Color3.fromRGB(180, 50, 50))

local toggled = false
MaskTop.MouseButton1Click:Connect(function()
	toggled = not toggled
	if toggled then
		MaskTop.Text = "Trade Masker : ON"
		TweenService:Create(MaskTop, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 180, 90)}):Play()
		TweenService:Create(MaskShadow, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 180, 90):Lerp(Color3.new(0,0,0), 0.45)}):Play()
	else
		MaskTop.Text = "Trade Masker : OFF"
		TweenService:Create(MaskTop, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(180, 50, 50)}):Play()
		TweenService:Create(MaskShadow, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(180, 50, 50):Lerp(Color3.new(0,0,0), 0.45)}):Play()
	end
end)

local FreezeTop, FreezeShadow = Create3DButton(Content, " FREEZE TRADE ❄️", UDim2.new(0.075, 0, 0, 75), UDim2.new(0.85, 0, 0, 36), Color3.fromRGB(40, 120, 255))

Close.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

local minimized = false
Minimize.MouseButton1Click:Connect(function()
	minimized = not minimized
	Content.Visible = not minimized
	TweenService:Create(MainFrame, TweenInfo.new(0.25), {
		Size = minimized and UDim2.new(0, 230, 0, 35) or UDim2.new(0, 230, 0, 170)
	}):Play()
end)

local dragging = false
local dragInput, dragStart, startPos

local function update(input)
	local delta = input.Position - dragStart
	MainFrame.Position = UDim2.new(
		startPos.X.Scale,
		startPos.X.Offset + delta.X,
		startPos.Y.Scale,
		startPos.Y.Offset + delta.Y
	)
end

Header.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = MainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

Header.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		update(input)
	end
end)
